from setuptools import setup, find_packages

setup(name="used-car-mlops-project", version="1.0", packages=find_packages())

# in the terminal, cd into root directory, in this case "used-car-project" 
# and from that directory, run "pip install -e ."